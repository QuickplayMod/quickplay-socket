version: '3.8'
services:
  socket:
    depends_on:
      - db
    image: robere2/quickplay-socket
    ports:
      - "54678:80"
      - "5858:5858"
    restart: unless-stopped
    volumes:
      - ../glyphs-volume:/usr/src/app/glyphs
      - .:/usr/src/app
      - /usr/src/app/node_modules
    command: npm run dev
    env_file:
      - .env
  db:
    image: mysql:8
    ports:
      - "3306:3306"
    command: --default-authentication-plugin=mysql_native_password
    restart: unless-stopped
    volumes:
      - ../db-volume:/var/lib/mysql
    env_file:
      - docker-db.env
